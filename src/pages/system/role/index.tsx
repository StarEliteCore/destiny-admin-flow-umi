import { Card, Typography } from 'antd';

import React from 'react';

export default (): React.ReactNode => (
  <div className="global-container">
    <Card>
      <Typography.Text strong>基于 role 开发，快速构建标准页面</Typography.Text>
    </Card>
  </div>
);
